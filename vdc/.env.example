# Vision Dataset Curation - Environment Configuration

# Base URL for deployment on specific HTTP routes
# Example: /demonstrations/vision-dataset-curation
# Leave empty for root deployment (/)
BASE_URL=

# Namespace and Domain Configuration
NAMESPACE=demonstrations
DOMAIN=teknoir.dev

# GCS Bucket Configuration
BUCKET_PREFIX=media/vision-dataset-curation

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DATABASE=vision_dataset_curation
MONGO_INITDB_ROOT_USERNAME=teknoir
MONGO_INITDB_ROOT_PASSWORD=teknoir123456!

# Flask Configuration
PORT=5000
HOST=0.0.0.0
FLASK_DEBUG=False

